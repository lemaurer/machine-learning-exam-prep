import type { Question } from "../types/question";
import { DERIVED, LINEAR_NN_SETUP, PCA_SETUP, opts, q } from "./hs21-common";

export const hs21Questions: Question[] = [
  q({ number:19, points:2, title:"PCA latent covariance", setup:PCA_SETUP,
    prompt:`What is the empirical covariance $\\Sigma_z^*$ of the latent variables $z_i^*$?`,
    options:opts(
      ["A",`$\\Sigma_z^*=\\Sigma_xW_*$`],
      ["B",`$\\Sigma_z^*=W_*\\Sigma_xW_*^\\top$`],
      ["C",`$\\Sigma_z^*=W_*^\\top\\Sigma_x$`],
      ["D",`$\\Sigma_z^*=W_*^\\top\\Sigma_xW_*$`],
    ), correct:["D"], topic:"Clustering & Dimensionality Reduction", difficulty:"Intermediate",
    explanation:`${DERIVED}Since $z_i^*=W_*^\\top x_i$, the latent covariance is $W_*^\\top\\Sigma_xW_*$.` }),
  q({ number:20, points:2, title:"Sampling from the PCA latent distribution", setup:PCA_SETUP,
    prompt:`We obtain new data by first sampling $s\\sim\\mathcal N(0,\\Sigma_z^*)$ in the latent space and then projecting the obtained sample to the original space $x_{new}=W_*s$. What is the distribution of $x_{new}$?`,
    options:opts(
      ["A",`$\\mathcal N(0,W_*\\Sigma_z^*W_*^\\top)$`],
      ["B",`$\\mathcal N(0,\\Sigma_x)$`],
      ["C",`$\\mathcal N(0,W_*^\\top\\Sigma_z^*W_*)$`],
      ["D",`$\\mathcal N(0,W_*W_*^\\top\\Sigma_x)$`],
    ), correct:["A"], topic:"Clustering & Dimensionality Reduction", difficulty:"Intermediate",
    explanation:`${DERIVED}A linear transformation $W_*s$ of a zero-mean Gaussian has covariance $W_*\\Sigma_z^*W_*^\\top$.` }),
  q({ number:21, points:2, title:"Direction of the principal eigenvector", setup:PCA_SETUP,
    prompt:`In Figure 1 we plot 1000 points. We apply PCA to those 1000 points. What is the direction of the principal eigenvector?`,
    options:opts(["A","A"],["B","B"],["C","C"],["D","D"]), correct:["A"], topic:"Clustering & Dimensionality Reduction", difficulty:"Foundation",
    figureNumber:1, figureAlt:"Scatter plot of 1000 points with four candidate directions A, B, C and D.", figureCaption:"Figure 1: Direction of principal eigenvector.",
    explanation:`${DERIVED}The cloud has its largest spread along the positively sloped direction marked A.` }),
  q({ number:22, points:1, title:"Linear PCA on the Swiss roll", setup:PCA_SETUP,
    prompt:`Figure 2 shows the Swiss roll dataset. All data points in this dataset lie on a 2D plane that has been wrapped around an axis. Linear (non-kernelized) PCA with $k=2$ can explain all sources of variance in this dataset ($C^*$ in Equation 3 is equal to 0).`,
    options:opts(["A","True"],["B","False"]), correct:["B"], topic:"Clustering & Dimensionality Reduction", difficulty:"Foundation",
    figureNumber:2, figureAlt:"Three-dimensional Swiss roll dataset.", figureCaption:"Figure 2: The Swiss roll dataset.",
    explanation:`${DERIVED}The Swiss roll is intrinsically two-dimensional but not contained in a two-dimensional linear subspace, so linear PCA with $k=2$ cannot reconstruct it with zero error.` }),
  q({ number:23, points:1, title:"PCA and k-means as reconstruction problems", setup:PCA_SETUP,
    prompt:`Both the PCA and the $k$-means problems can be formulated as

$$\\min_{W,z_1,\\ldots,z_n}\\sum_{i=1}^n\\|Wz_i-x_i\\|_2^2,$$

albeit, with different constraints on the matrix $W$ and vectors $z_1,\\ldots,z_n$.`,
    options:opts(["A","True"],["B","False"]), correct:["A"], topic:"Clustering & Dimensionality Reduction", difficulty:"Intermediate",
    explanation:`${DERIVED}PCA constrains $W$ to an orthonormal linear subspace, while k-means uses cluster centers with discrete one-hot assignments $z_i$.` }),
  q({ number:24, points:1, title:"Gaussian-kernel PCA feature dimension", setup:PCA_SETUP,
    prompt:`If we use the Gaussian kernel for kernel PCA, we implicitly perform PCA on an infinite-dimensional feature space.`,
    options:opts(["A","True"],["B","False"]), correct:["A"], topic:"Clustering & Dimensionality Reduction", difficulty:"Foundation",
    explanation:`${DERIVED}The Gaussian RBF kernel corresponds to an infinite-dimensional feature map.` }),
  q({ number:25, points:1, title:"Nonlinear autoencoder compression", setup:PCA_SETUP,
    prompt:`If we use neural network autoencoders with nonlinear activation functions, we seek to compress the data with a nonlinear map.`,
    options:opts(["A","True"],["B","False"]), correct:["A"], topic:"Neural Networks", difficulty:"Foundation",
    explanation:`${DERIVED}Nonlinear activations allow the encoder to represent nonlinear compression maps.` }),

  q({ number:26, points:1, title:"Depth-one linear network", setup:LINEAR_NN_SETUP,
    prompt:`With depth $K=1$, we recover linear regression (with no bias term).`,
    options:opts(["A","True"],["B","False"]), correct:["A"], topic:"Neural Networks", difficulty:"Foundation",
    explanation:`${DERIVED}For $K=1$, $F(x)=W_1x$ is exactly a linear predictor without an intercept.` }),
  q({ number:27, points:1, title:"Uniqueness in a deep linear network", setup:LINEAR_NN_SETUP,
    prompt:`For $K=2$ there is a unique pair of matrices $W_1,W_2$ that minimizes $\\ell$.`,
    options:opts(["A","True"],["B","False"]), correct:["B"], topic:"Neural Networks", difficulty:"Foundation",
    explanation:`${DERIVED}Factorizations of the same effective linear map are generally non-unique.` }),
  q({ number:28, points:1, title:"Expressivity of deeper linear networks", setup:LINEAR_NN_SETUP,
    prompt:`Networks with increasing depth $K$ allow one to model more complex relationships between $x$ and $y$.`,
    options:opts(["A","True"],["B","False"]), correct:["B"], topic:"Neural Networks", difficulty:"Foundation",
    explanation:`${DERIVED}A product of linear maps is still a linear map, so adding depth without nonlinear activations does not enlarge the represented function class.` }),
  q({ number:29, points:1, title:"Shape of the final linear-network layer", setup:LINEAR_NN_SETUP,
    prompt:`$W_K$ must be a row vector.`,
    options:opts(["A","True"],["B","False"]), correct:["A"], topic:"Neural Networks", difficulty:"Foundation",
    explanation:`${DERIVED}The network output is scalar, so the last linear map must have one output row.` }),
  q({ number:30, points:3, title:"Gradient with respect to the last scalar layer", setup:LINEAR_NN_SETUP,
    prompt:`You plan to train this model with stochastic gradient descent and batch size 1. In each batch, you minimize $\\tilde\\ell_x(F)=(y-F(x))^2$, for a fixed data point $x$. For simplicity, suppose $K=3$ and $W_3$ is a scalar.

Then, $\\partial\\tilde\\ell_x/\\partial W_3$ is equal to`,
    options:opts(
      ["A",`$-2(y-F(x))(W_2W_1x)$`],
      ["B",`$2(y-F(x))(W_2W_1x)$`],
      ["C",`$(y-F(x))(W_2W_1x)$`],
      ["D",`$-(y-F(x))(W_2W_1x)$`],
      ["E",`$-2(y-F(x))$`],
      ["F",`$2(y-F(x))$`],
      ["G",`$(y-F(x))$`],
      ["H",`$-(y-F(x))$`],
    ), correct:["A"], topic:"Neural Networks", difficulty:"Intermediate",
    explanation:`${DERIVED}Differentiate $(y-W_3W_2W_1x)^2$ with respect to the scalar $W_3$.` }),
  q({ number:31, points:2, title:"Backpropagation gradient for the first layer", setup:LINEAR_NN_SETUP,
    prompt:`Again consider the loss calculated on a fixed data point $x\\in\\mathbb R^{d_0}$,

$$\\tilde\\ell_x(F)=(y-F(x))^2.$$

We use backpropagation to compute $\\partial\\tilde\\ell_x/\\partial W_1$. Suppose that $z_1(x)=W_1x\\in\\mathbb R^{d_1}$. From previous steps in the backpropagation algorithm you know that $\\partial\\tilde\\ell_x/\\partial z_1(x)=a\\in\\mathbb R^{d_1}$. Please assume $a$ and $x$ are column vectors, i.e., $a\\in\\mathbb R^{d_1\\times1}$ and $x\\in\\mathbb R^{d_0\\times1}$.

Compute $\\partial\\tilde\\ell_x/\\partial W_1\\in\\mathbb R^{d_1\\times d_0}$.`,
    options:opts(
      ["A",`$2ax^\\top$`], ["B",`$-2ax^\\top$`], ["C",`$ax^\\top$`], ["D",`$-ax^\\top$`],
      ["E",`$2a(W_1x)^\\top$`], ["F",`$-2a(W_1x)^\\top$`], ["G",`$a(W_1x)^\\top$`], ["H",`$-a(W_1x)^\\top$`],
    ), correct:["C"], topic:"Neural Networks", difficulty:"Intermediate",
    explanation:`${DERIVED}For $z_1=W_1x$, the matrix derivative is the outer product of the upstream gradient and input: $a x^\\top$.` }),
  q({ number:32, points:2, title:"SGD update for the first layer", setup:LINEAR_NN_SETUP,
    prompt:`Which of the following describes one iteration of a stochastic gradient descent update (still batch size 1 with single data point $x$) on $W_1$ with step size $\\alpha$?`,
    options:opts(
      ["A",`$W_1\\leftarrow W_1-\\alpha\\frac{\\partial\\tilde\\ell_x}{\\partial W_1}$`],
      ["B",`$W_1\\leftarrow W_1+\\alpha\\frac{\\partial\\tilde\\ell_x}{\\partial W_1}$`],
      ["C",`$W_1\\leftarrow W_1-\\alpha\\frac{\\partial z_1(x)}{\\partial W_1}$`],
      ["D",`$W_1\\leftarrow W_1+\\alpha\\frac{\\partial z_1(x)}{\\partial W_1}$`],
    ), correct:["A"], topic:"Optimization & Model Selection", difficulty:"Foundation",
    explanation:`${DERIVED}SGD subtracts the stochastic loss gradient with respect to the parameter being updated.` }),
  q({ number:33, points:1, title:"Minibatch size and gradient variance",
    prompt:`Increasing the minibatch size in stochastic gradient descent (SGD) lowers the variance of gradient estimates (assuming data points in the mini-batch are selected independently).`,
    options:opts(["A","True"],["B","False"]), correct:["A"], topic:"Optimization & Model Selection", difficulty:"Foundation",
    explanation:`${DERIVED}Averaging more independent stochastic gradients reduces their variance.` }),
  q({ number:34, points:1, title:"Per-iteration SGD decrease",
    prompt:`For a minibatch size of 1, SGD is guaranteed to decrease the loss in every iteration.`,
    options:opts(["A","True"],["B","False"]), correct:["B"], topic:"Optimization & Model Selection", difficulty:"Foundation",
    explanation:`${DERIVED}A stochastic gradient step can increase the full empirical loss on a particular iteration.` }),
  q({ number:35, points:1, title:"Global convergence of SGD with momentum",
    prompt:`There exists a fixed learning rate $\\eta>0$ such that SGD with momentum is guaranteed to converge to a global minimum of the empirical risk for any architecture of a neural network and any dataset.`,
    options:opts(["A","True"],["B","False"]), correct:["B"], topic:"Optimization & Model Selection", difficulty:"Foundation",
    explanation:`${DERIVED}General neural-network objectives are non-convex and no fixed learning rate gives such a universal global-convergence guarantee.` }),
  q({ number:36, points:1, title:"Cross-entropy task type",
    prompt:`The cross entropy loss is designed for regression tasks, where the goal is to predict arbitrary real-valued labels.`,
    options:opts(["A","True"],["B","False"]), correct:["B"], topic:"Neural Networks", difficulty:"Foundation",
    explanation:`${DERIVED}Cross entropy is primarily a classification/probabilistic-label loss rather than a loss for arbitrary real-valued regression targets.` }),
];