import { CV_SETUP, DERIVED, NN_BACKPROP_SETUP, NN_INIT_SETUP, REGULARIZATION_ORTHO_SETUP, REGULARIZATION_SETUP, TRAIN_TEST_SETUP, opts, q } from "./fs23-common";

export const fs23Questions = [
  q({ number: 14, points: 1, title: "Convexity of neural-network training", setup: NN_BACKPROP_SETUP, prompt: "Minimizing the squared loss $\\ell$ with respect to $W$ given (any choice of) a single input $x$ and (any choice of) output $y$ is a convex optimization problem.", options: opts(["A","True"],["B","False"]), correct:["B"], topic:"Neural Networks", difficulty:"Foundation", explanation: DERIVED + "[[B]]. A multilayer ReLU network is nonconvex in its weights, even for a single training pair." }),
  q({ number: 15, points: 2, title: "Forward/backward computational cost", setup: NN_BACKPROP_SETUP, prompt: "Which of the provided choices makes the following statement true, assuming all layers have the same width? ‘The computational costs with respect to the number of layers $L$ …’", options: opts(["A","… scale linearly for the forward pass and linearly for the backward pass."],["B","… scale linearly for the forward pass and quadratically for the backward pass."],["C","… scale quadratically for the forward pass and linearly for the backward pass."],["D","… scale quadratically for the forward pass and quadratically for the backward pass."]), correct:["A"], topic:"Neural Networks", difficulty:"Foundation", explanation: DERIVED + "[[A]]. With fixed layer width, each layer contributes constant-order work relative to $L$, so both passes scale linearly in the number of layers." }),
  q({ number: 16, points: 2, title: "He initialization", setup: NN_INIT_SETUP, prompt: "A common scheme is to initialize the weights with independent Gaussians, $W^{(k)}_{ij}\\sim\\mathcal{N}(0,2/p_k)$. Recall that $p_k$ denotes the input dimension of layer $k$. What is true for a forward pass under this initialization strategy immediately after initializing the weights, for random inputs $x\\sim\\mathcal{N}(0,I)$?", options: opts(["A","As $k$ increases, the variance of the activations $v^{(k)}$ approximately increases."],["B","As $k$ increases, the variance of the activations $v^{(k)}$ is approximately constant."],["C","As $k$ increases, the variance of the activations $v^{(k)}$ approximately decreases."],["D","One cannot say anything."]), correct:["B"], topic:"Neural Networks", difficulty:"Intermediate", explanation: DERIVED + "[[B]]. He initialization uses variance $2/p_k$ to compensate for the variance reduction caused by ReLU and approximately preserve activation variance across layers." }),
  q({ number: 17, points: 4, title: "Zero weight initialization", setup: NN_INIT_SETUP, prompt: "Instead of relying on an established strategy, we try our own initialization scheme. We initialize the network weights with $W^{(k)}_{ij}=0$ for all layers $k$ and entries $i,j$. Which of the following statements about the parameter gradient of the $k$-th layer $\\nabla_{W^{(k)}}f(x,W)$ is true at initialization for all inputs $x$?", options: opts(["A","For each layer $k\\in\\{1,\\ldots,L\\}$, the gradient is the zero matrix."],["B","For layer $k=1$, the gradient is the zero matrix. For $k>1$, the gradient may have nonzero entries."],["C","For layer $k=L$, the gradient is the zero matrix. For $k<L$, the gradient may have nonzero entries."],["D","For each layer $k\\in\\{1,\\ldots,L\\}$, the gradient matrix may have nonzero entries."]), correct:["A"], topic:"Neural Networks", difficulty:"Advanced", explanation: DERIVED + "[[A]]. All hidden activations are zero and $h'(0)=0$, so every parameter derivative vanishes at the all-zero initialization." }),
  q({ number: 18, points: 4, title: "All-ones weight initialization", setup: NN_INIT_SETUP, prompt: "Next, we initialize the network weights with $W^{(k)}_{ij}=1$ for all layers $k$ and entries $i,j$. Which of the following statements about the gradient matrix $\\nabla_{W^{(k)}}f(x,W)$ of the $k$-th layer is true at initialization for all inputs $x$?", options: opts(["A","For each $k\\in\\{1,\\ldots,L\\}$, all entries of the gradient matrix are equal."],["B","For each $k\\in\\{1,\\ldots,L\\}$, any two rows of the gradient matrix are equal."],["C","For $k=1$, any two rows of the gradient matrix are equal. For each $k>1$, two rows of the gradient matrix may differ."],["D","For $k=L$, any two rows of the gradient matrix are equal. For each $k<L$, two rows of the gradient matrix may differ."]), correct:["B"], topic:"Neural Networks", difficulty:"Advanced", explanation: DERIVED + "[[B]]. Symmetric all-ones weights make neurons within each layer indistinguishable, so the backpropagated signal is identical across rows. Entries within a row need not all match because the input coordinates can differ." }),
  q({ number: 19, points: 2, title: "Dropout regularization", setup: NN_INIT_SETUP, prompt: "Which of the following is not true about the dropout regularization technique?", options: opts(["A","The activations $v^{(k)}$ are multiplied by random zero masks only during optimization and not when using the neural network to make test-time predictions."],["B","Dropout can prevent overfitting and thus improve test-time generalization."],["C","The weights $W$ are multiplied by the dropout probability during optimization to account for the dropped hidden units."]), correct:["C"], topic:"Neural Networks", difficulty:"Foundation", explanation: DERIVED + "[[C]]. Dropout masks activations during training. Weight/activation scaling is used to match train and test expectations, but the stated multiplication of weights during optimization is not the dropout rule." }),
  q({ number: 20, points: 1, title: "Train versus test error", setup: TRAIN_TEST_SETUP, prompt: "Without further assumptions on the ground truth, which of the following statements is correct about the test error and training error?", options: opts(["A","Training error on average under-estimates the test error."],["B","Training error is an unbiased estimator of the test error."],["C","Training error on average over-estimates the test error."]), correct:["A"], topic:"Optimization & Model Selection", difficulty:"Foundation", explanation: DERIVED + "[[A]]. The model is fit on the training data, making training error optimistically biased relative to error on unseen data." }),
  q({ number: 21, points: 2, title: "Effect of more training data", setup: TRAIN_TEST_SETUP, prompt: "Which of the following statements is correct about the test error and training error? The number of parameters of the model are kept fixed to some constant number. On average over the draws of the data,", options: opts(["A","increasing the number of training points reduces the training error to zero."],["B","increasing the number of testing points reduces the test error to zero."],["C","increasing the number of training points decreases the gap between train and test error."],["D","increasing the number of training points increases the gap between train and test error."]), correct:["C"], topic:"Optimization & Model Selection", difficulty:"Intermediate", explanation: DERIVED + "[[C]]. With fixed model capacity, more training data reduces overfitting and typically narrows the expected train–test gap." }),
  q({ number: 22, points: 2, title: "Choosing K in cross-validation", setup: CV_SETUP, prompt: "According to Figure 2, which choice of $K$ leads to the best choice of $\\lambda$ in the sense of minimizing true generalization error?", options: opts(["A","$K_1$"],["B","$K_2$"],["C","$K_3$"]), correct:["B"], topic:"Optimization & Model Selection", difficulty:"Intermediate", figureNumber:2, figureAlt:"Figure 2: CV curves for K1, K2, K3 and the true generalization-error curve as functions of lambda.", figureCaption:"Figure 2: Cross-validation errors and true generalization error versus $\\lambda$.", explanation: DERIVED + "[[B]]. In the figure, the minimum of the dotted $CV_{K_2}$ curve aligns with the minimum of the solid gray true-risk curve." }),
  q({ number: 23, points: 2, title: "Effect of increasing K in K-fold CV", setup: CV_SETUP, prompt: "Let $\\hat f_i$, $i\\in[K]$ denote the model that was trained without the $i$-th fold, and $\\hat L_i$ denote the empirical error of $\\hat f_i$ on the $i$-th fold. What are the consequences of increasing $K$ in $K$-fold cross-validation?", options: opts(["A","For every fold $i\\in[K]$, the empirical validation error $\\hat L_i$ becomes a more accurate approximation of the true generalization error $L(\\hat f_i;P)$."],["B","Each $\\hat f_i$ becomes closer to the model $\\hat f$ trained on the entire dataset."],["C","Model selection becomes a computationally lighter task."]), correct:["B"], topic:"Optimization & Model Selection", difficulty:"Intermediate", explanation: DERIVED + "[[B]]. Increasing $K$ increases each training split, making $\\hat f_i$ closer to the full-data model. The validation folds become smaller and computation increases." }),
  q({ number: 24, points: 1, title: "Least-squares estimator", setup: REGULARIZATION_SETUP, prompt: "What is the least square estimator $\\hat w^{LS}$ in terms of the matrix $X$ and vector $y$?", options: opts(["A","$X^\\top(XX^\\top)^{-1}y$"],["B","$(X^\\top X)^{-1}X^\\top y$"]), correct:["B"], topic:"Kernels & Regression", difficulty:"Foundation", explanation: DERIVED + "[[B]]. Since $X$ has full column rank and $n>d$, the normal equations give $\\hat w^{LS}=(X^\\top X)^{-1}X^\\top y$." }),
  q({ number: 25, points: 2, title: "Bias of ridge and least squares", setup: REGULARIZATION_ORTHO_SETUP, prompt: "Which of the following statements is correct about the ridge and least squares estimators? Recall: An estimator $\\hat w$ is unbiased if $\\mathbb{E}[\\hat w]=w^\\star$.", options: opts(["A","$\\hat w^{ridge}$ is biased, $\\hat w^{LS}$ is unbiased."],["B","$\\hat w^{ridge}$ is unbiased, $\\hat w^{LS}$ is biased."],["C","Both are unbiased."],["D","Both are biased."]), correct:["A"], topic:"Kernels & Regression", difficulty:"Intermediate", explanation: DERIVED + "[[A]]. Least squares is unbiased under zero-mean noise. Ridge shrinks coefficients toward zero and is generally biased." }),
  q({ number: 26, points: 2, title: "Variance of ridge and least squares", setup: REGULARIZATION_ORTHO_SETUP, prompt: "Which of the following statements is correct about the ridge and least squares estimators? Recall: The variance of an estimator $\\hat w$ is defined as $\\operatorname{Var}(\\hat w)=\\mathbb{E}[\\|\\hat w-\\mathbb{E}[\\hat w]\\|^2]$.", options: opts(["A","$\\operatorname{Var}(\\hat w^{LS})<\\operatorname{Var}(\\hat w^{ridge})$"],["B","$\\operatorname{Var}(\\hat w^{ridge})<\\operatorname{Var}(\\hat w^{LS})$"],["C","$\\operatorname{Var}(\\hat w^{LS})=\\operatorname{Var}(\\hat w^{ridge})$"],["D","We can’t say anything."]), correct:["B"], topic:"Kernels & Regression", difficulty:"Intermediate", explanation: DERIVED + "[[B]]. Under $X^\\top X=I$, ridge multiplies the noisy least-squares estimate by $1/(1+\\lambda)$, reducing its variance." }),
  q({ number: 27, points: 4, title: "Best subset selector under orthogonal design", setup: REGULARIZATION_ORTHO_SETUP, prompt: "What is $\\hat w^{BS}$ in terms of $\\hat w^{LS}$?\n\nHint: If a term in the loss does not depend on $w$, it does not affect the minimizer. Also recall that $X^\\top X=I$.", options: opts(["A","$\\arg\\min_{\\|w\\|_0=k}\\|w-\\hat w^{LS}\\|_2^2+y^\\top Xw$"],["B","$\\arg\\min_{\\|w\\|_0=k}\\|w-\\hat w^{LS}\\|_2^2-y^\\top Xw$"],["C","$\\arg\\min_{\\|w\\|_0=k}\\|w-\\hat w^{LS}\\|_2^2+w^\\top X^\\top Xw$"],["D","$\\arg\\min_{\\|w\\|_0=k}\\|w-\\hat w^{LS}\\|_2^2$"]), correct:["D"], topic:"Kernels & Regression", difficulty:"Advanced", explanation: DERIVED + "[[D]]. Expanding $\\|y-Xw\\|^2$ with $X^\\top X=I$ and completing the square leaves $\\|w-\\hat w^{LS}\\|^2$ plus a constant independent of $w$." }),
  q({ number: 28, points: 2, title: "Identify lasso from coefficient scatter", setup: REGULARIZATION_ORTHO_SETUP, prompt: "In Figure 3, which estimator corresponds to $\\hat w^A$?", options: opts(["A","$\\hat w^{BS}$"],["B","$\\hat w^{lasso}(0.5)$"],["C","$\\hat w^{ridge}(0.5)$"],["D","$\\hat w^{ridge}(1)$"]), correct:["B"], topic:"Kernels & Regression", difficulty:"Advanced", figureNumber:3, figureAlt:"Figure 3: coefficient scatter comparing least squares with estimators wA and wB.", figureCaption:"Figure 3: Coefficient scatters related to Question 28.", explanation: DERIVED + "[[B]]. $\\hat w^A$ soft-thresholds small least-squares coefficients to zero while shrinking the larger ones, which is the characteristic lasso pattern." }),
  q({ number: 29, points: 2, title: "Identify ridge from coefficient scatter", setup: REGULARIZATION_ORTHO_SETUP, prompt: "In Figure 4, which estimator corresponds to $\\hat w^C$?", options: opts(["A","$\\hat w^{BS}$"],["B","$\\hat w^{lasso}(0.5)$"],["C","$\\hat w^{ridge}(0.5)$"],["D","$\\hat w^{ridge}(1)$"]), correct:["D"], topic:"Kernels & Regression", difficulty:"Advanced", figureNumber:4, figureAlt:"Figure 4: coefficient scatter comparing least squares with estimators wC and wD.", figureCaption:"Figure 4: Coefficient scatters related to Question 29.", explanation: DERIVED + "[[D]]. With $X^\\top X=I$, ridge gives $\\hat w^{ridge}(\\lambda)=\\hat w^{LS}/(1+\\lambda)$. The $w^C$ points are approximately half of least squares, so $\\lambda=1$." }),
];
