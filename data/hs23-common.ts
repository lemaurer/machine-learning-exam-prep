import type { Difficulty, Question, QuestionOption, Topic } from "../types/question";

export type OptionId = QuestionOption["id"];
export const EXAM_ID = "HS23";
export const EXAM_LABEL = "HS23 · January 2024";
export const opts = (...items: Array<[OptionId, string]>): QuestionOption[] => items.map(([id, text]) => ({ id, text }));

export function q({ number, points, title, prompt, options, correct, topic, difficulty, setup, multipleSelect = false, figureNumber, figureAlt, figureCaption, explanation }: {
  number: number; points: number; title: string; prompt: string; options: QuestionOption[]; correct: OptionId[];
  topic: Topic; difficulty: Difficulty; setup?: string; multipleSelect?: boolean; figureNumber?: number; figureAlt?: string; figureCaption?: string; explanation: string;
}): Question {
  return {
    id: `hs23-q${String(number).padStart(2, "0")}`, examId: EXAM_ID, examLabel: EXAM_LABEL, number, title, setup, prompt, options,
    correctOptionId: correct[0], correctOptionIds: correct, multipleSelect, explanation, topic, difficulty,
    source: `HS23 · Question ${number} · ${points} point${points === 1 ? "" : "s"}`, figureNumber, figureAlt, figureCaption,
  };
}

export const GD_TRAJECTORY_SETUP = "Suppose we are running the gradient descent (GD) algorithm to minimize a convex loss function. We run 4 experiments to try out 4 different step sizes $\\eta_1,\\ldots,\\eta_4$, and plot the trajectories in Figure 1. All experiments start from the same initial point (marked with a white diamond) and run for 100 iterations. The steps of the algorithms are shown as black dots and the consecutive steps are connected with lines. The contour plot shows how the loss function looks like: darker colors show lower function values. The global minimum is marked with a white star.";
export const RELU_OPT_SETUP = "Let $f:\\mathbb{R}^d\\to\\mathbb{R}$ be the output of a ReLU network with 2 neurons,\n\\[\nf(x;\\theta)=v_1\\sigma(x^\\top w_1)+v_2\\sigma(x^\\top w_2),\n\\]\nwhere $x\\in\\mathbb{R}^d$, $\\theta=(v_1,v_2,w_1^\\top,w_2^\\top)\\in\\mathbb{R}^{2+2d}$, and $\\sigma(z)=\\max(0,z)$. Suppose we have a single training point $(x,y)$, fix $(w_1,w_2)$, and optimize $(v_1,v_2)$ using\n\\[\nL(v_1,v_2)=\\left(y-v_1\\sigma(x^\\top w_1)-v_2\\sigma(x^\\top w_2)\\right)^2.\n\\]\nGradient descent uses\n\\[\n\\begin{pmatrix}v_1^t\\\\v_2^t\\end{pmatrix}\n=\n\\begin{pmatrix}v_1^{t-1}\\\\v_2^{t-1}\\end{pmatrix}\n-\\eta\\nabla L(v_1^{t-1},v_2^{t-1}).\n\\]";
export const CLASSIFICATION_SETUP = "Given a dataset $\\{(x_1,y_1),\\ldots,(x_n,y_n)\\}$ where $x_i\\in\\mathbb{R}^d$ and $y_i\\in\\{-1,1\\}$, consider the classification model $\\operatorname{sign}(f_w(\\cdot))$ with $f_w(x)=w^\\top x$ and loss\n\\[\nL(w)=\\sum_{i=1}^n\\ell(f_w(x_i),y_i).\n\\]\nRecall that $\\operatorname{sign}(\\bullet)=-1$ if $\\bullet<0$, $\\operatorname{sign}(\\bullet)=1$ if $\\bullet>0$, and $\\operatorname{sign}(\\bullet)=0$ if $\\bullet=0$.";
export const CLASSIFIER_FIGURE_SETUP = "We compare six binary classification methods on the same half-moons dataset with an outlier: logistic regression, a decision tree, K-nearest neighbors with $K=7$, an SVM with quadratic kernel, and SVMs with RBF kernels using a larger and a smaller length-scale parameter. Figure 2 shows panels A--F. Each panel corresponds to exactly one method.";
export const SYMMETRY_SETUP = "Consider a feedforward neural network with one hidden layer and no biases,\n\\[\nf(x,W)=W_2\\sigma(W_1x),\n\\]\nwhere $\\sigma(\\cdot)=\\max(0,\\cdot)$ is applied element-wise, $x\\in\\mathbb{R}^2$, and $W_1,W_2\\in\\mathbb{R}^{2\\times2}$. We have one input/output pair $x_1=(1,1)^\\top$, $y_1=(1,2)^\\top$ and minimize\n\\[\nl(W)=\\|f(x_1,W)-y_1\\|_2^2.\n\\]\nInitialize\n\\[\nW_1^{(0)}=\\begin{pmatrix}1&0\\\\0&1\\end{pmatrix},\\qquad\nW_2^{(0)}=\\begin{pmatrix}1&1\\\\2&2\\end{pmatrix},\n\\]\nand run gradient descent with $\\eta=0.01$.";
export const PCA_GENERAL_SETUP = "We are given centered data $D=\\{x_1,\\ldots,x_n\\}$ with $x_i\\in\\mathbb{R}^{1000}$ and empirical covariance\n\\[\n\\Sigma=\\frac1n\\sum_{i=1}^n x_ix_i^\\top\n=\\sum_{i=1}^{1000}\\lambda_i v_iv_i^\\top,\n\\]\nwhere $v_1,\\ldots,v_{1000}$ are orthonormal and $\\lambda_1>\\lambda_2>\\cdots>\\lambda_{1000}>0$. For a fixed unit vector $w$, the regularized one-dimensional reconstruction solves\n\\[\n\\tilde{x}_i=\\arg\\min_{x:\\,x=zw}\\|x-x_i\\|_2^2+\\gamma\\|x\\|_2^2,\\qquad \\gamma>0.\n\\]\nWe also consider the one-dimensional problem\n\\[\nw^*\\in\\arg\\min_{\\|w\\|_2=1}w^\\top\\Sigma w.\n\\]";
export const AUTOENCODER_SETUP = "Consider a deep linear autoencoder with $L>1$ layers and no biases,\n\\[\nf(x,W)=W^{(L)}W^{(L-1)}\\cdots W^{(1)}x,\n\\]\nwith squared reconstruction loss\n\\[\n\\ell(x,W)=\\|x-f(x,W)\\|_2^2.\n\\]\nThe centered dataset is $x_1,\\ldots,x_n\\in\\mathbb{R}^{1000}$. The weights are initialized from $\\mathcal{N}(0,1)$ and optimized with 100,000 stochastic-gradient steps.";
export const ELEVATOR_SETUP = "We observe $n$ independent elevator waiting times $D=\\{t_1,\\ldots,t_n\\}$ with $t_i\\sim U[0,\\theta]$ and $\\theta>0$. The density is\n\\[\np(t\\mid\\theta)=\\frac1\\theta I(\\{0\\leq t\\leq\\theta\\}).\n\\]\nFor Questions 36--37, the prior is $\\theta\\sim\\operatorname{Pa}(\\mu,k)$ with known $\\mu,k>0$ and density\n\\[\np(\\theta)=\\frac{k\\mu^k}{\\theta^{k+1}}I(\\mu\\leq\\theta).\n\\]";
export const CHEATING_SETUP = "We model whether a friend is cheating using a latent Bernoulli variable $C$ with $p=P(C=1)$. The observed coin result is $X\\in\\{0,1\\}$, where 0 is tails and 1 is heads. If $C=0$, the coin is fair:\n\\[\nP(X=1\\mid C=0)=0.5.\n\\]\nIf $C=1$, then\n\\[\nP(X=1\\mid C=1)=0.5+q,\\qquad q\\in(0,0.5].\n\\]\nWe estimate $p$ and $q$ using soft EM. After observing 3 tails and 5 heads, initialize $p_0=0.5$ and $q_0=0.3$.";
