import type { Difficulty, Question, QuestionOption, Topic } from "../types/question";

export type OptionId = QuestionOption["id"];
export const EXAM_ID = "FS24";
export const EXAM_LABEL = "FS24 · August 2024";
export const opts = (...items: Array<[OptionId, string]>): QuestionOption[] => items.map(([id, text]) => ({ id, text }));

export function q({ number, points, title, prompt, options, correct, topic, difficulty, setup, multipleSelect = false, figureNumber, figureAlt, figureCaption, explanation }: {
  number: number; points: number; title: string; prompt: string; options: QuestionOption[]; correct: OptionId[];
  topic: Topic; difficulty: Difficulty; setup?: string; multipleSelect?: boolean; figureNumber?: number; figureAlt?: string; figureCaption?: string; explanation: string;
}): Question {
  return {
    id: `fs24-q${String(number).padStart(2, "0")}`, examId: EXAM_ID, examLabel: EXAM_LABEL, number, title, setup, prompt, options,
    correctOptionId: correct[0], correctOptionIds: correct, multipleSelect, explanation, topic, difficulty,
    source: `FS24 · Question ${number} · ${points} point${points === 1 ? "" : "s"}`, figureNumber, figureAlt, figureCaption,
  };
}

export const LOGISTIC_SETUP = "You are fitting a linear model $f(x)=wx$ to the dataset $(x_1,y_1),$ $(x_2,y_2)$ with $x_1=y_1=-1$ and $x_2=y_2=1$. You minimize the simple logistic loss\n$$\n\\sum_{i=1}^n\\log(1+e^{-y_i f(x_i)}).\n$$";
export const CLASSIFIER_SETUP = "In this section, we compare different binary classification methods on a concrete dataset. Figure 1 visualizes the dataset and the result of applying the different methods for classification: one class is visualized with dark circles, and the other with light diamonds. Further, the shades represent the trained classifiers: the area with the dark background is predicted as the class of the dark circles, whereas the area with the light background is predicted as the class of the light diamonds.\n\nWe consider the following binary classification methods: logistic regression, a one hidden layer neural network with ReLU activations and $\\leq 10$ neurons in the hidden layer, and kernel SVM.";
export const NN_FIG_SETUP = "Consider the following simple neural network with three hidden layers.\n\nPlease answer the following questions.";
export const CNN_SEQUENCE_SETUP = "Although commonly associated with image classification tasks, convolutional neural networks (CNNs) have proven to be valuable tools for sequence modeling and forecasting, given the right modifications.";
export const LINEAR_REG_SETUP = "Consider a dataset $\\{(x_1,y_1),\\ldots,(x_n,y_n)\\}\\subset\\mathbb{R}\\times\\mathbb{R}$ and the hypothesis space of affine functions $G=\\{w_0+w_1x:w_0,w_1\\in\\mathbb{R}\\}$. The error of a solution $f\\in G$, i.e., $f(x)=w_0+w_1x$ for some $w_0,w_1\\in\\mathbb{R}$ is given by\n$$\nL(w_0,w_1)=\\frac{1}{2n}\\sum_{i=1}^n( y_i-w_0-w_1x_i)^2.\n$$";
export const KMEANS_SETUP = "Consider the following data matrix containing five observations:\n$$\nX=\\begin{pmatrix}\n-4&-3&1&2&3\\\\\n-2&0&2&5&2\n\\end{pmatrix}^{\\!\\top}.\n$$\nWe want to separate the points into $k=2$ clusters using Lloyd's algorithm, with centroids initialized as $\\mu_1^{(0)}=(1,0)^\\top$ and $\\mu_2^{(0)}=(4,4)^\\top$.";
export const PCA_FIG_SETUP = "PCA is applied to the 2-dimensional dataset shown in Figure 2. The vectors indicate possible directions of principal components.";
export const PCA_COV_SETUP = "Now consider another 2-dimensional dataset to which PCA was applied. Projecting the data on the subspace spanned by $(3\\;4)^\\top$ preserves the most variance. The empirical variance of the data projected on one of the principal components is 25, and projected on the other it is 50.";
export const UNIFORM_SETUP = "We collected 2-dimensional data\n$$\nD=\\{(x_i,y_i)\\}_{i=1}^3=\\{(1,0),(3,1),(5,4)\\}\n$$\nwhere the distribution of the data and the relationship between $x_i$ and $y_i$ is unknown. We use probabilistic modeling to identify the underlying data-distribution.\n\nWe model the distribution $P_{X;\\theta_1}=\\mathcal{U}(0,\\theta_1)$, and the conditional distribution, $P_{Y\\mid X;\\theta_2}=\\mathcal{U}(x-\\theta_2,x+\\theta_2)$ as uniform distributions with the parameters $\\theta_1,\\theta_2>0$, and where the PDF of $\\mathcal{U}(a,b)$ is\n$$\n\\mathcal{U}(t;a,b)=\n\\begin{cases}\n\\frac{1}{b-a} & \\text{if }a\\leq t\\leq b,\\\\\n0 & \\text{otherwise.}\n\\end{cases}\n$$";
export const EM_SETUP = "We perform a physics experiment several times. For each selected temperature $t_i\\in\\mathbb{R}$, we observe an outcome $y_i\\in\\mathbb{R}$. After $N$ experiments, the scatter plot of our observations looks as shown in the figure.\n\nWe deduce that a phase shift occurs, where the outcome qualitatively changes depending on the temperature regime. We model whether or not a phase shift was achieved in experiment $i$ by $z_i\\in\\{0,1\\}$ with\n$$\np(z_i=1)=\\frac{\\alpha}{\\alpha+1}\n$$\nand parameter $\\alpha>0$. Given the phase $z_i$, we model the outcome $y_i$ as a linear function of $t_i$ with phase-specific slope $w_{z_i}$ that is observed under Gaussian noise:\n$$\np(y_i\\mid z_i;t_i)=\\mathcal{N}(y_i;w_{z_i}t_i,1).\n$$\nWe apply the soft EM algorithm to infer the parameters $\\theta:=\\{\\alpha,w_0,w_1\\}$.\n\nHint: The Gaussian PDF with mean $\\mu$ and variance $\\sigma^2$ is given by\n$$\n\\mathcal{N}(x;\\mu,\\sigma^2)=\\frac{1}{\\sigma\\sqrt{2\\pi}}\\exp\\left(-\\frac{(x-\\mu)^2}{2\\sigma^2}\\right).\n$$";
export const EM_MSTEP_SETUP = "We perform a physics experiment several times. For each selected temperature $t_i\\in\\mathbb{R}$, we observe an outcome $y_i\\in\\mathbb{R}$. After $N$ experiments, the scatter plot of our observations looks as shown in the figure.\n\nWe deduce that a phase shift occurs, where the outcome qualitatively changes depending on the temperature regime. We model whether or not a phase shift was achieved in experiment $i$ by $z_i\\in\\{0,1\\}$ with\n$$\np(z_i=1)=\\frac{\\alpha}{\\alpha+1}\n$$\nand parameter $\\alpha>0$. Given the phase $z_i$, we model the outcome $y_i$ as a linear function of $t_i$ with phase-specific slope $w_{z_i}$ that is observed under Gaussian noise:\n$$\np(y_i\\mid z_i;t_i)=\\mathcal{N}(y_i;w_{z_i}t_i,1).\n$$\nWe apply the soft EM algorithm to infer the parameters $\\theta:=\\{\\alpha,w_0,w_1\\}$.\n\nHint: The Gaussian PDF with mean $\\mu$ and variance $\\sigma^2$ is given by\n$$\n\\mathcal{N}(x;\\mu,\\sigma^2)=\\frac{1}{\\sigma\\sqrt{2\\pi}}\\exp\\left(-\\frac{(x-\\mu)^2}{2\\sigma^2}\\right).\n$$\n\nSuppose we estimated $\\gamma_z(y_i):=p(z_i=z\\mid y_i;t_i,\\theta)$ for $z\\in\\{0,1\\}$ and each experiment $i$ in the E-step. In the M-step, we now estimate $\\theta$ using these posterior estimates.";
