import type { Difficulty, Question, QuestionOption, Topic } from "../types/question";

export type OptionId = QuestionOption["id"];
export const EXAM_ID = "HS22";
export const EXAM_LABEL = "HS22 · February 2023";
export const opts = (...items: Array<[OptionId, string]>): QuestionOption[] => items.map(([id, text]) => ({ id, text }));

export function q({ number, points, title, prompt, options, correct, topic, difficulty, setup, multipleSelect = false, figureNumber, figureAlt, figureCaption, secondFigureNumber, secondFigureAlt, secondFigureCaption, explanation }: {
  number: number; points: number; title: string; prompt: string; options: QuestionOption[]; correct: OptionId[];
  topic: Topic; difficulty: Difficulty; setup?: string; multipleSelect?: boolean; figureNumber?: number; figureAlt?: string; figureCaption?: string;
  secondFigureNumber?: number; secondFigureAlt?: string; secondFigureCaption?: string; explanation: string;
}): Question {
  return {
    id: `hs22-q${String(number).padStart(2, "0")}`, examId: EXAM_ID, examLabel: EXAM_LABEL, number, title, setup, prompt, options,
    correctOptionId: correct[0], correctOptionIds: correct, multipleSelect, explanation, topic, difficulty,
    source: `HS22 · Question ${number} · ${points} point${points === 1 ? "" : "s"} · answer derived (supplied question pack has no solution key)`,
    figureNumber, figureAlt, figureCaption, secondFigureNumber, secondFigureAlt, secondFigureCaption,
  };
}

export const DERIVED = "Derived answer (the supplied HS22 question pack contains no official solution key): ";

export const AFFINE_SETUP = "We say $f:\\mathbb{R}^d\\to\\mathbb{R}$ is an affine function if there are $a\\in\\mathbb{R}^d$, $b\\in\\mathbb{R}$ such that $$f(w)=a^\\top w+b.\\tag{Aff}$$";

export const AFFINE_GD_SETUP = `${AFFINE_SETUP}\n\nConsider the Gradient Descent algorithm for minimizing the affine function $f$ in (Aff):\n$$w^{t+1}=w^t-\\eta\\nabla f(w^t),\\qquad \\eta>0\\text{ is the step-size}.\\tag{GD}$$`;

export const REG_AFFINE_SETUP = `${AFFINE_SETUP}\n\nWe now consider regularized affine functions: For a regularization parameter $\\delta>0$, define\n$$f_\\delta(w):=f(w)+\\frac{\\delta}{2}\\|w\\|^2=a^\\top w+b+\\frac{\\delta}{2}\\|w\\|^2.\\tag{Aff_\\delta}$$`;

export const REG_AFFINE_GD_SETUP = `${REG_AFFINE_SETUP}\n\nConsider the Gradient Descent algorithm for minimizing the regularized affine function in $(\\mathrm{Aff}_\\delta)$:\n$$w^{t+1}=w^t-\\eta\\nabla f_\\delta(w^t),\\qquad \\eta>0\\text{ is the step-size}.\\tag{GD_\\delta}$$`;

export const OPT_TRAJECTORY1_SETUP = "In this question, we explore different optimization algorithms. Specifically, we consider Gradient Descent (GD) with fixed step-size, and GD with momentum (GD+Mo) and fixed step-size. Figure 1 shows iterations of four different algorithms on the “banana” function. All the algorithms start from the same initial point (marked in the figure with a white diamond) and run for 100 iterations. The iterates of the algorithms are shown as black dots and the consecutive iterates are connected with lines. The following algorithms with their specific parameters are used. Below, $\\eta$ denotes the step-size, and $m$ denotes the momentum parameter.\n\nAlgorithm 1: GD ($\\eta=0.70$)\n\nAlgorithm 2: GD ($\\eta=0.86$)\n\nAlgorithm 3: GD+Mo ($\\eta=2.6$, $m=0.9$)\n\nAlgorithm 4: GD+Mo ($\\eta=2.6$, $m=0.99$)";

export const OPT_TRAJECTORY2_SETUP = "Figure 2 shows iterations of three different algorithms on another function. All the algorithms start from the top right (marked with a gray diamond) and run for 100 iterations. This time, we also test out the ADAM algorithm. Recall that ADAM is an adaptive optimization algorithm, and adapts its step-size and momentum per each coordinate at each iteration. The following algorithms with their specific parameters are used:\n\nAlgorithm 1: GD ($\\eta=10$)\n\nAlgorithm 2: GD+Mo ($\\eta=10$, $m=0.9$)\n\nAlgorithm 3: ADAM ($\\eta=1$)";

export const CONV_SETUP = "We have trained a Convolutional Neural Network on a dataset of abstract paintings. To understand the effect of the trained filters, we convolve four of them ($W_1,\\ldots,W_4$) with a test painting $X$ and get four output images, see Figures 3 and 4 below. Your task is to match the output images to their corresponding filters.\n\nFigure 3: The input image $X$ with four filters $W_1,\\ldots,W_4$. All filters are of the same size ($15\\times15$), and the values corresponding to pixel colors are shown in the color bar on the right. For example, any pixel with value less than $-1$ is colored as black and the background color of the image $X$ is $0$.\n\nFigure 4: Resulting activations when convolving different filters with the input image $X$. These pictures follow the same coloring as in Figure 3: white pixels have higher values and dark pixels show lower values.";

export const LINEAR_NN_SETUP = "A linear neural network is a neural network with fully connected layers and identity activation functions. More formally, a $K$-layer linear neural network is defined via $K$ matrices $W:=(W_1,W_2,\\ldots,W_K)$, where $W_i\\in\\mathbb{R}^{d_i\\times d_{i-1}}$ is the weight matrix of the $i$-th layer. For an input vector $x\\in\\mathbb{R}^{d_0}$, the output of the neural network is\n$$f_W(x):=W_KW_{K-1}\\cdots W_1x.$$\nWe always assume that the last layer $W_K$ is a row vector, meaning that $f_W:\\mathbb{R}^{d_0}\\to\\mathbb{R}$. Given a dataset $\\{(x_i,y_i)\\}_{i=1}^n$, we define the training loss\n$$L(W):=\\frac{1}{2n}\\sum_{i=1}^n(f_W(x_i)-y_i)^2.$$\nMoreover, For two linear neural networks $W=(W_1,\\ldots,W_K)$ and $W'=(W'_1,\\ldots,W'_K)$ with equal shapes, and a real number $a\\in\\mathbb{R}$, we define\n$$aW:=(aW_1,aW_2,\\ldots,aW_K),$$\nand\n$$W+W':=(W_1+W'_1,W_2+W'_2,\\ldots,W_K+W'_K).$$\nNote that two linear neural networks have equal shapes if they have the same number of layers and the weight matrices of corresponding layers have the same dimensions.";

export const LINEAR_NN_GRAD_SETUP = `${LINEAR_NN_SETUP}\n\nWe now consider the case where $K=2$; that is, $f_W(x)=W_2W_1x$. When running gradient descent to minimize $L(W)$, we need the gradients of each layer. The next two questions ask you to compute them.\n\nMake sure that the gradients have the correct shape, such that the gradient descent updates can be written as\n$$W_i^{(t+1)}\\leftarrow W_i^{(t)}-\\eta\\nabla_{W_i}L(W^{(t)}).$$`;

export const RELU_NN_SETUP = "Recall that the ReLU activation function is $\\sigma(z):=\\max(0,z)$. We consider an artificial neural network with ReLU activations:\n$$f_W:\\mathbb{R}^{d_0}\\to\\mathbb{R},\\qquad f_W(x)=W_K\\sigma(W_{K-1}\\sigma(W_{K-2}\\cdots\\sigma(W_1x)))$$\nwhere $W_i$'s are the weight matrix of the $i$-th layer, and let $W:=(W_1,W_2,\\ldots,W_K)$. Here, the activation function $\\sigma$ is applied coordinate-wise. Let $\\{(x_i,y_i)\\}_{i=1}^n$ be a dataset, and consider the training loss\n$$L(W):=\\frac{1}{2n}\\sum_{i=1}^n(f_W(x_i)-y_i)^2.$$";

export const CLASSIFICATION_SETUP = "In this section, we consider training and evaluation of a neural network on a binary classification task. We are interested in loss functions of the form\n$$L_\\square(f,\\mathcal{D},\\lambda)=\\frac{1}{|\\mathcal{D}|}\\sum_{(x,y)\\in\\mathcal{D}}\\ell_\\square(f(x),y)+\\lambda r(f)$$\nwhere $f$ is a neural network and $r(f)$ is the sum of the squares of its weights, $\\mathcal{D}$ is a dataset, $\\ell_\\square$ quantifies how well $f$ fits to the data with $\\square\\in\\{01,\\mathrm{hinge},\\exp,\\log\\}$, and $\\lambda>0$ is the regularization parameter. Table 1 below details the choices for $\\ell_\\square$:\n\n$\\ell_{01}(z,y)=\\mathbf{1}\\{-zy>0\\}$; $\\ell_{\\mathrm{hinge}}(z,y)=\\max(0,1-zy)$; $\\ell_{\\exp}(z,y)=e^{-zy}$; $\\ell_{\\log}(z,y)=-y\\log z-(1-y)\\log(1-z)$.\n\nFor example, if $\\mathcal{D}=\\{(x_1,y_1),\\ldots,(x_n,y_n)\\}$, we have\n$$L_{\\exp}(f,\\mathcal{D},3)=\\frac1n\\sum_{i=1}^ne^{-f(x_i)\\cdot y_i}+3r(f).$$\nAssume we are given a training set $\\mathcal{D}_{\\mathrm{train}}$ and a validation set $\\mathcal{D}_{\\mathrm{valid}}$, both consisting of i.i.d. samples from the same distribution. We only use the training set for training, and evaluate our trained neural network via the validation set.";

export const VALIDATION_VALUES_SETUP = `${CLASSIFICATION_SETUP}\n\nWe now optimize $L_{\\log}(f,\\mathcal{D}_{\\mathrm{train}},5)$ and obtain a network $\\hat f$. We then calculate the validation loss for a couple of $\\ell_\\square$'s, resulting in the following values:\n$$L_{\\exp}(\\hat f,\\mathcal{D}_{\\mathrm{valid}},5)=1.52198,\\qquad L_{\\exp}(\\hat f,\\mathcal{D}_{\\mathrm{valid}},0)=1.44339$$\n$$L_{\\log}(\\hat f,\\mathcal{D}_{\\mathrm{valid}},5)=0.8357,\\qquad L_{\\log}(\\hat f,\\mathcal{D}_{\\mathrm{valid}},0)=0.75711$$\n$$L_{\\mathrm{hinge}}(\\hat f,\\mathcal{D}_{\\mathrm{valid}},5)=0.89148,\\qquad L_{\\mathrm{hinge}}(\\hat f,\\mathcal{D}_{\\mathrm{valid}},0)=0.81289.$$`;

export const KMEANS_SETUP = "We have a dataset with five points $\\{x_1,\\ldots,x_5\\}$, where $x_i\\in\\mathbb{R}^{d_0}$. Our goal is to group this data into 3 clusters. We know that this data is best clustered if we first apply a feature map $\\phi:\\mathbb{R}^{d_0}\\to\\mathbb{R}^d$, with $d\\gg d_0$, and then perform K-means clustering on the mapped data. Algorithm 1 shows the pseudocode of this approach.\n\nAlgorithm 1\n\nstart with some initial clusters $C_1,C_2,C_3$\n\nrepeat\n\nfor $i=1,\\ldots,5$ do\n\nfind distance of $\\phi(x_i)$ to all cluster centers\n\nassign $x_i$ to the closest cluster\n\nend for\n\nupdate $C_1,C_2,C_3$, each containing the points assigned to that cluster.\n\nuntil clusters do not change.\n\nreturn $C_1,C_2,C_3$.\n\nSince $d$ is very large, calculating the distances in Algorithm 1 comes with a large computational cost. Instead, we use the kernel function corresponding to the feature map $\\phi$. Suppose the kernel matrix (or the Gram matrix) for the dataset is given as\n$$K=\\begin{pmatrix}2&-1&1&0&0\\\\-1&9&0&-1&-1\\\\1&0&7&1&-3\\\\0&-1&1&1&0\\\\0&-1&-3&0&4\\end{pmatrix},$$\nwhere $K_{i,j}=\\phi(x_i)^\\top\\phi(x_j)$. Let the initial clusters be $C_1=\\{1,2\\}$, $C_2=\\{3\\}$, $C_3=\\{4,5\\}$. We define the center of each cluster $C_k$ as\n$$\\mu_k:=\\frac1{|C_k|}\\sum_{j\\in C_k}\\phi(x_j)\\in\\mathbb{R}^d.$$";

export const KRR_SETUP = "We are given the dataset $\\{(x_i,y_i)\\}_{i=1}^n$ with $x_i\\in\\mathbb{R}^d$ and $y_i\\in\\mathbb{R}$, and we want to do Ridge regression. By introducing a valid kernel $k$ and its corresponding feature map $\\phi:\\mathbb{R}^d\\to\\mathbb{R}^D$ (where $D$ can be $\\infty$), we consider the optimization problem\n$$\\hat w:=\\arg\\min_{w\\in\\mathbb{R}^D}\\frac1n\\|y-\\Phi w\\|^2+\\lambda\\|w\\|^2,\\tag{1}$$\nwhere $\\Phi$ is an $n\\times D$ matrix whose $i$-th row is $\\phi(x_i)^\\top$, $y=(y_1,\\ldots,y_n)$, and $\\lambda>0$ is the regularization constant. An alternative to solving (1) is to first compute the kernel matrix $K\\in\\mathbb{R}^{n\\times n}$, with $K_{i,j}=k(x_i,x_j)$, and then solve the following optimization problem:\n$$\\min_{\\alpha\\in\\mathbb{R}^n}\\frac1n\\|y-K\\alpha\\|^2+\\lambda\\alpha^\\top K\\alpha.\\tag{2}$$\nLet $\\hat\\alpha$ denote the minimizer of (2), if it exists.";

export const ONE_CLASS_SETUP = "In outlier detection, the goal is to determine whether a new test sample is inlier, namely whether it is a member of the class represented by the training data, or whether it is outlier. In particular, we assume that all/most of the training data are from the inlier class, and want to model them in such a way that for new unseen test points, we can tell whether they “look like” these points (i.e., they are inlier), or they are different (i.e, outlier).\n\nLet $\\{x_1,x_2,\\ldots,x_n\\}$ be the training data for the outlier detection problem (all supposedly belonging to one class — the inlier class). One way to formulate outlier detection using Support Vector Machines is to have the goal of finding a decision hyperplane which passes through the origin, and for which all the training points are on one side of it. We also want to maximize the distance between the decision hyperplane and the data points. With this in mind, we consider the following (constrained) optimization problem:\n$$\\hat w:=\\arg\\min_{w\\in\\mathbb{R}^d}\\frac12\\|w\\|^2\\qquad\\text{such that }w^\\top x_i\\ge1,\\ \\forall i=1,\\ldots,n.\\tag{3}$$";

export const ONE_CLASS_REST_SETUP = `${ONE_CLASS_SETUP}\n\nFor the rest of the questions of this section, assume that the optimization problem (3) has a solution $\\hat w\\ne0$.`;

export const EM_SETUP = "Getting the first “view” for a video posted on social media takes a random time, which follows an exponential distribution with an unknown parameter $\\lambda>0$. Recall that a random variable $X$ has an exponential distribution with parameter $\\lambda$, if it has the probability density\n$$p_X(x)=\\begin{cases}\\lambda e^{-\\lambda x}&x\\ge0,\\\\0&x<0.\\end{cases}$$\nWe submitted $n$ videos and recorded the time that it took for each of them to be viewed for the first time. The recorded times are $t_{1:n}=(t_1,\\ldots,t_n)$. Here, $t_i$ is the time that it took for the $i$-th video to be viewed for the first time.";

export const EM_CENSORED_SETUP = `${EM_SETUP}\n\nWe then submit another $m$ videos to the same social media platform, and at time $\\tau$, we check whether they got any views or not. It turns out that none of the newly submitted videos have been watched by time $\\tau$. We use EM algorithm to incorporate this new information and update our previous estimate of $\\lambda$.\n\nLet $z_{1:m}=(Z_1,\\ldots,Z_m)$ be the hidden variables denoting the time that it would take for the new videos to be viewed for the first time. Our observation that none of the videos are viewed by time $\\tau$ can be equivalently stated as “the random variable $Z_i\\ge\\tau$, for all $i=1,\\ldots,m$.” Denote by $\\lambda^{(j)}$ our estimation of $\\lambda$ at iteration $j$ of the EM algorithm.\n\nAt iteration $(j+1)$, we perform the E-step by computing:\n$$Q(\\lambda;\\lambda^{(j)}):=\\mathbb{E}_{z_{1:m}}\\left[\\log p(t_{1:n},z_{1:m};\\lambda)\\mid t_{1:n},\\lambda^{(j)}\\right].$$`;
